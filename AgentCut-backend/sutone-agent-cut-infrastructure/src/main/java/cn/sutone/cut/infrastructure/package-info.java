/**
 * 基础实施层（infrastructure）。
 *
 * <p>存放领域端口的具体实现：MyBatis 仓储、MinIO 对象存储、RocketMQ 消息、
 * Python 分析服务客户端（IVideoAnalysisClient）、FFmpeg 渲染引擎等。</p>
 * <p>骨架阶段为空壳，具体实现由后续 worker 在此填充。</p>
 */
package cn.sutone.cut.infrastructure;
