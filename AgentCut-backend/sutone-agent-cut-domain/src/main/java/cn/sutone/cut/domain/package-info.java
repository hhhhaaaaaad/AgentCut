/**
 * 领域层（核心）。
 *
 * <p>后续按业务域拆分子包：project / asset / analysis / plan / task / render，
 * 每个子包内按 model(entity|aggregate|valobj) / adapter(port|repository) / service 组织。</p>
 * <p>骨架阶段为空壳，业务领域模型由后续 worker 在此填充。</p>
 */
package cn.sutone.cut.domain;
