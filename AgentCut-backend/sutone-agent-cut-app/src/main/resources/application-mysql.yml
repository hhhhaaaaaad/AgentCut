# MySQL / MyBatis 持久化 profile（SPRING_PROFILES_ACTIVE=mysql 时启用）
# 连接本机 MySQL84（端口 3306），建表脚本见 docs/schema.sql
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/agentcut?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    map-underscore-to-camel-case: true
