server:
  port: 8080

spring:
  application:
    name: sutone-agent-cut
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

# 日志、数据源、Redis、RocketMQ、MinIO 等中间件配置由后续 worker 补充
logging:
  level:
    root: info
